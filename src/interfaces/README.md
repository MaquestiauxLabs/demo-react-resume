# Interfaces

This directory contains TypeScript interface definitions used throughout the application. These interfaces help ensure type safety and consistency in data structures across different components and modules.
